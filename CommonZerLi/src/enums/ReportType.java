package enums;

/**
 * in this class we define reports type enumus
 * @author Dvir bublil
 * */
public enum ReportType {

	ORDERS("ORDERS",0),
	INCOME("INCOME",1),
	SPEICEL("SPEICEL",2),
	SURVEY("SURVEY",3),
	COMPLAINTS("COMPLAINTS",4);
	
	ReportType(String string, int i) {
	}
}
